(function () {
    // Automatic Redirect Logic - DISABLED
    /*
    const minDelay = 600000; // 10 minutes
    const maxDelay = 1200000; // 20 minutes
    const randomDelay = Math.floor(Math.random() * (maxDelay - minDelay + 1)) + minDelay;

    console.log('Auto-redirect scheduled in ' + (randomDelay / 1000) + ' seconds...');

    setTimeout(function () {
        const urlParams = window.location.search;
        window.location.href = '/redirect.php' + urlParams;
    }, randomDelay);
    */
})();
